export type HeroStat = {
  value: string;
  label: string;
};

export type ProcessStep = {
  title: string;
  description: string;
  icon: string;
  accent: string;
};

export type ServiceHighlight = {
  title: string;
  detail: string;
  points: string[];
  badge: string;
};

